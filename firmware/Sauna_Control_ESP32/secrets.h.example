// Copy to secrets.h and set real values. Never commit secrets.h.
// secrets.h is in .gitignore.

#define SECRET_WIFI_SSID       "YourWiFiSSID"
#define SECRET_WIFI_PASS       "YourWiFiPassword"

// API_HOST: use ONE of these:
//   HTTP (dev):  "74.208.133.101:8000"   -> http://IP:8000
//   HTTPS:      "sauna.yourdomain.com"   -> https://domain (port 443)
// No http:// or https:// prefix.
#define SECRET_API_HOST        "sauna.yourdomain.com"

#define SECRET_DEVICE_ID       "sauna-1"
#define SECRET_DEVICE_TOKEN    "your-device-token-from-env"
