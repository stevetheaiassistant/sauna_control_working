# Copy to .env and set real values. Never commit .env.
# On VPS: use this file or set in systemd (EnvironmentFile=/opt/sauna/.env).

SAUNA_DEVICE_TOKEN=changeme
SAUNA_APP_TOKEN=changeme
SAUNA_DEVICE_ID=sauna-1
SAUNA_DB=sauna.db
